const fs = require('fs')
// If canvas not available, just create placeholder PNGs
// We'll use the SVG logo from the existing landing page
console.log('Place icon-192.png and icon-512.png in public/')
console.log('Use the MonElu hemicycle SVG from the landing page, export at those sizes')
